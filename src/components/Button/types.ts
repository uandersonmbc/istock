import { TouchableOpacityProps } from 'react-native';
import { TypesColos } from './../../shared/colors';
export interface Props {
    icon?: string;
    loading?: boolean;
    variant?: TypesColos;
}
